# מדיניות עדכון — מפתחות להצלחה

נקודת הייחוס לעדכונים רציפים היא:

- Windows: `appId` קבוע `il.co.mkreceipt.desktop`, החל מגרסה `1.1.6`.
- Android: package קבוע `il.mkreceiptpro.android`; ה־APK הבא יקבל מספר בנייה גבוה מ־9 באופן אוטומטי דרך EAS.

## Windows

בגרסה הבאה יש לפתוח את קובץ ה־`Setup.exe` החדש **מבלי להסיר קודם** את "מפתחות להצלחה". מתקין NSIS יזהה את אותה זהות התקנה, יחליף את קובצי התוכנה וישמור את נתוני המשתמש. הפעולה עדיין תוצג כמתקין, אך אינה יוצרת תוכנה נוספת.

אין לשנות את `appId`, את `executableName`, את מצב ההתקנה למשתמש יחיד, או להוסיף `nsis.guid` חדש. שינוי כזה ינתק את רצף העדכונים.

## Android

יש להתקין כל APK חדש מעל האפליקציה הקיימת. מאחר שה־package נשאר קבוע ואותו מפתח חתימה מרוחק של Expo משמש לבנייה, Android יציג "עדכון" וישמור את נתוני האפליקציה. EAS מעלה אוטומטית את `versionCode` בכל בניית APK או חנות.

אין לשנות את `android.package` או לאפס את מפתח החתימה של Expo.
