# MK Receipt Pro

Private source repository for MK Receipt Pro.

## Applications
- `apps/windows` — Windows / Electron desktop application
- `apps/android` — Android / Expo application
- `supabase` — database migrations and security hardening

Security-sensitive project. Do not commit secrets, service-role keys, keystores, signing credentials, `.env` files, or production tokens.
