import fs from "node:fs";const p=JSON.parse(fs.readFileSync("package.json","utf8"));const a=JSON.parse(fs.readFileSync("app.json","utf8"));const checks=[[p.version==="1.0.5","version 1.0.5"],[a.expo.version==="1.0.5","app version"],[a.expo.android.versionCode===7,"versionCode 7"],[p.scripts["verify:device-management"],"device management audit"]];let ok=0;for(const [x,n] of checks){console.log(`${x?"PASS":"FAIL"} ${n}`);if(x)ok++;}console.log(`Android 1.0.5 release: ${ok}/${checks.length}`);if(ok!==checks.length)process.exit(1);
