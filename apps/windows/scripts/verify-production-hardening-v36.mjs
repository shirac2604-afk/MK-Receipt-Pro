import fs from "node:fs";const pkg=JSON.parse(fs.readFileSync("package.json","utf8"));const ipc=fs.readFileSync("apps/desktop/electron/ipc/databaseHandlers.ts","utf8");const ui=fs.readFileSync("apps/desktop/renderer/src/main.tsx","utf8");const checks=[[/^\d+\.\d+\.\d+$/.test(pkg.version),"version"],[ipc.includes("receiptIssueInFlight"),"IPC issue mutex"],[ipc.includes("receiptCancelInFlight"),"IPC cancel mutex"],[ipc.includes("CLOUD_CONNECTION_REQUIRED_FOR_CANCELLATION"),"offline cancellation blocked"],[ui.includes("issueLock=useRef(false)"),"UI issue lock"],[ui.includes("friendlyCloudError"),"friendly errors"],[ui.includes("issueLock.current=true"),"immediate issue lock"],[ui.includes("issueLock.current=false"),"lock released"]];let ok=0;for(const[p,l]of checks){console.log(p?"PASS":"FAIL",l);if(p)ok++}console.log(`Windows production hardening: ${ok}/${checks.length}`);if(ok!==checks.length)process.exit(1);
