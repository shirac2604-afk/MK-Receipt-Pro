import fs from "node:fs";
const files={repo:fs.readFileSync("packages/database/src/repositories/ReceiptRepository.ts","utf8"),issue:fs.readFileSync("packages/application/src/receipts/IssueReceiptService.ts","utf8"),db:fs.readFileSync("packages/database/src/DatabaseService.ts","utf8"),cloud:fs.readFileSync("apps/desktop/electron/main/SupabaseCloudService.ts","utf8"),ipc:fs.readFileSync("apps/desktop/electron/ipc/databaseHandlers.ts","utf8")};
const checks=[[files.cloud.includes("reserve_receipt_number"),"atomic reservation RPC"],[files.cloud.includes("issue_receipt_from_reservation"),"cloud issuance RPC"],[files.issue.includes("cloudReceiptNumber?:number"),"forced cloud number support"],[files.repo.includes("adoptCloudReceiptNumber"),"local sequence adopts cloud number"],[files.ipc.includes("CLOUD_CONNECTION_REQUIRED_FOR_RECEIPT"),"offline Windows issuance blocked"],[files.ipc.includes("cloudReceipt.receiptNumber"),"cloud number mirrored locally"],[files.cloud.includes('from("receipt-documents").upload'),"PDF uploaded to shared storage"],[files.cloud.includes("pdf_storage_key"),"cloud PDF key persisted"]];let ok=0;for(const [p,l] of checks){console.log(p?"PASS":"FAIL",l);if(p)ok++}console.log(`Cloud receipt numbering: ${ok}/${checks.length}`);if(ok!==checks.length)process.exit(1);
