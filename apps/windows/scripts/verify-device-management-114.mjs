import fs from "node:fs";const svc=fs.readFileSync("apps/desktop/electron/main/SupabaseCloudService.ts","utf8");const ipc=fs.readFileSync("apps/desktop/electron/ipc/databaseHandlers.ts","utf8");const pre=fs.readFileSync("apps/desktop/electron/preload/preload.ts","utf8");const ui=fs.readFileSync("apps/desktop/renderer/src/main.tsx","utf8");const checks=[[svc.includes("async listDevices"),"device list service"],[svc.includes('rpc("revoke_device"'),"secure revoke RPC"],[svc.includes("targetDeviceId===deviceId"),"current-device service guard"],[ipc.includes("cloud-account:revoke-device"),"revoke IPC"],[pre.includes("revokeDevice"),"preload API"],[ui.includes("cloud-device-list"),"device list UI"],[ui.includes("נרשם:"),"created date"],[ui.includes("פעילות אחרונה:"),"last seen"],[ui.includes("d.id!==status.deviceId"),"current-device UI guard"],[ui.includes(">נתק</button>"),"revoke button"]];let ok=0;for(const [x,n] of checks){console.log(`${x?"PASS":"FAIL"} ${n}`);if(x)ok++;}console.log(`Windows device management: ${ok}/${checks.length}`);if(ok!==checks.length)process.exit(1);
