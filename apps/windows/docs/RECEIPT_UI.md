# Receipt UI 0.8.0

The first production-facing receipt screen includes customer details, service description, payment details, live preview, quick amounts, secure issuing and a success screen. The renderer sends only validated business input through the preload API. It cannot access SQLite or arbitrary file paths.
