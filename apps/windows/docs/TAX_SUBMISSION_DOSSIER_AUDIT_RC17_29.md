# ביקורת תיק הגשה לרשות המסים — rc.17.29

## תנאי מוכנות שנבדקים אוטומטית

- דוח PDF רשמי של הסימולטור, בפורמט PDF תקין.
- תוצאת סימולטור שסומנה כתקינה ותואמת לכמויות ההפקה.
- לפחות 2,000 רשומות בדוח הסימולטור.
- `INI.TXT` ו־`BKMVDATA.TXT`.
- דוחות `REPORT-2.6.pdf` ו־`REPORT-5.4.pdf`.
- בדיקת Preflight תקינה.
- בדיקת יצירת PDF תקינה.
- ביקורת כותרות המבנה האחיד תקינה.
- מספר רישום תוכנה שאינו `00000000`.
- מספר יצרן תקף בקובץ `INI.TXT`.
- פרטי יצרן ואיש קשר מלאים.

## תוצר חדש

בתיק ההגשה נוצר `11-REGISTRATION-READINESS.json`, שמציג `ready`, את כל הבדיקות ואת רשימת החסמים שנותרו.

## הערה

הבדיקה האוטומטית אינה מאמתת שה־PDF הגיע בפועל מאתר רשות המסים ואינה מחליפה בדיקה מקצועית או החלטת רשות המסים.
